{
  "name": "@abundbank/client-gateway",
  "version": "1.0.0",
  "description": "Client Gateway - API Gateway for client applications",
  "main": "src/server.js",
  "private": true,
  "scripts": {
    "dev": "nodemon src/server.js",
    "start": "node src/server.js",
    "test": "jest",
    "build": "echo 'No build step required'"
  },
  "dependencies": {
    "@abundbank/shared": "workspace:*",
    "express": "^5.2.1",
    "compression": "^1.8.1",
    "http-proxy-middleware": "^2.0.6",
    "swagger-ui-express": "^5.0.1",
    "dotenv": "^17.2.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.3"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}